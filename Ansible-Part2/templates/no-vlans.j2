{# This removes the VLAN database entries #}
{% for num in range(4004,4008) %}
no vlan {{num}}
{% endfor %}
{# This removes the SVIs #}
{% for num in range(4004,4008) %}
no interface vlan{{num}}
{% endfor %}
